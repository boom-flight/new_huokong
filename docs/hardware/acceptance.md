# 硬件验收记录

- 板卡编号：待上板验证
- 测试日期：待上板验证
- 操作人员：待上板验证

| 检查项 | 验收要求 | 结果 |
| --- | --- | --- |
| BMI088 PS 选择 | 通过电气测量确认 PS 处于 SPI 模式。 | 待上板验证 |
| 机体系安装方向 | 实测右手系有符号轴置换；确认当前 identity 映射，或按实测结果修改映射。 | 待上板验证 |
| Chip ID | accel 为 `0x1E`，gyro 为 `0x0F`。 | 待上板验证 |
| Data-ready 速率 | accel 为 800 Hz、gyro 为 1000 Hz，各自误差不超过 5%。 | 待上板验证 |
| 事件耐久 | 连续运行 10 分钟，正常工况 IMU overrun 为 0。 | 待上板验证 |
| 遥测耐久 | 以 200 Hz 连续运行 10 分钟，接收端 CRC error 为 0。 | 待上板验证 |
| 静止姿态 | 校准后水平放置，roll/pitch 约在 2 degree 以内。 | 待上板验证 |
| 相对 yaw | 无不连续跳变，并记录漂移测量结果。 | 待上板验证 |
| 恢复 | 实测传感器复位、接收端断开和整板重新上电后的恢复行为。 | 待上板验证 |

主机仿真、单元测试、交叉构建、尺寸检查和链接所有权检查均不能让本表任何物理项目变为 PASS。只有在目标板上实际执行对应步骤后，才能填写带日期、板卡编号、操作人员和原始证据的结果；未执行项目必须继续标记为“待上板验证”。

硬件原始资料见[硬件资料来源](source/README.md)。
